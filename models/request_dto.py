class Request:
        def __init__(self,id,user_id,amount,reason,status):
            self.id = id
            self.user_id = user_id
            self.amount = amount
            self.reason = reason
            self.status = status